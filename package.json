{
  "name": "leadrift-workspace",
  "private": true,
  "description": "Run API + Vite UI together from the repo root",
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
