# Optional. API origin only (no /api suffix). In Vite dev, leave unset to use same-origin /api (proxy).
# Production / custom API host:
# VITE_API_URL=https://your-api.example.com
# Local API without proxy:
# VITE_API_URL=http://localhost:5000
